# Optionnel — sans ces variables, le stockage en mémoire est utilisé (local uniquement)
# Sur Vercel en production, crée une base Upstash Redis gratuite :
# https://vercel.com/marketplace/upstash

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
